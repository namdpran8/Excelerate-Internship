import 'package:flutter/material.dart';

// Temporary placeholder to be replaced by the assigned screen owner.
class ProgramDetailsScreen extends StatelessWidget {
  const ProgramDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Program Details Screen')),
      body: const Center(child: Text('Program Details Screen')),
    );
  }
}
